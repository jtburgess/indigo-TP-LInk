<?xml version="1.0"?>
<!-- If your plugin defines device types, you can define them here.

	 Devices must specify their type (via the type attribute):

		relay (ON/OFF/STATUS)
		dimmer (ON/OFF/DIM/BRIGHTEN/SET BRIGHTNESS/STATUS)
		speedcontrol (ON/OFF/SET SPEED LEVEL/SET SPEED INDEX/INCREASE/DECREASE/STATUS)
		sensor (ON/OFF/STATUS)
		thermostat
		sprinkler
		custom

	 The first six inherit states, actions and events from Indigo. You can
	 also specify your own custom actions and additional states, if needed.

	 The two Device examples below specify the relay and dimmer types.
	 
	 Devices must also specify a unique typeID - so, for instance, if you define
	 2 relay type devices then your plugin can distinguish between the two by
	 the typeID.
-->

<Devices>
	<!-- I'm going to start with one relay device type for a Kasa SmartPlug.
	   Others can be added as I learn what they need to be.
	-->
	<Device type="relay" id="SmartPlug">
		<Name>SmartPlug</Name>
		<ConfigUI>
			<!-- There is one required parameter - the IP address of the device
			-->
			<Field id="address" type="textfield" defaultValue="192.168.0.0">
				<Label>Module IP Address:</Label>
			</Field>
		</ConfigUI>
		<States>
			<!-- there are no custom states
			-->
		</States>
	</Device>

	<Device type="relay" id="hs110">
			<Name>HS110 SmartPlug w/Energy Monitoring</Name>
			<ConfigUI>
				<Field id="address" type="textfield" defaultValue="192.168.0.0">
					<Label>Module IP Address:</Label>
				</Field>
				<Field id="configured" type="checkbox" hidden="true" defaultValue="false">
				</Field>
				<Field id="Name" type="textfield" visibleBindingId='configured' visibleBindingValue="true">
					<Label>Name</Label>
				</Field>
				<Field id="Model" type="textfield" visibleBindingId='configured' visibleBindingValue="true">
					<Label>Model</Label>
				</Field>
				<Field id="Alias" type="textfield" visibleBindingId='configured' visibleBindingValue="true">
					<Label>Alias</Label>
				</Field>
				<Field id="MAC" type="textfield" visibleBindingId='configured' visibleBindingValue="true">
					<Label>MAC Address</Label>
				</Field>
			</ConfigUI>
			<States>
				<State id="User">
					<ValueType>String</ValueType>
					<TriggerLabel>Username</TriggerLabel>
					<ControlPageLabel>User Name</ControlPageLabel>
				</State>
				<State id="Bind">
					<ValueType boolType="YesNo">Boolean</ValueType>
					<TriggerLabel>Bind State</TriggerLabel>
					<ControlPageLabel>Bind State</ControlPageLabel>
				</State>
				<State id="CurWatts">
					<ValueType>Float</ValueType>
					<TriggerLabel>Current Watts</TriggerLabel>
					<ControlPageLabel>Current Watts</ControlPageLabel>
				</State>
				<State id="CurVolts">
					<ValueType>Float</ValueType>
					<TriggerLabel>Current Volts</TriggerLabel>
					<ControlPageLabel>Current Volts</ControlPageLabel>
				</State>
				<State id="CurAmps">
					<ValueType>Float</ValueType>
					<TriggerLabel>Current Amps</TriggerLabel>
					<ControlPageLabel>Current Amps</ControlPageLabel>
				</State>
			</States>
		</Device>
</Devices>
